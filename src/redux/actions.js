import {} from './types';

const {} = require('./service');

export function changeSearchValue(text) {
  return {
  }
}




export function fetchP()  {
  /*return function(dispatch) {
    
    dispatch(startFixedBidFetch());
    return getFixedBids()
      .then(response => {
        if (response.status !== 200) {
          throw new Error("Error fetching fixed bid API");
        } 
        else{
          dispatch(finishFixedBidFetch(response.data));
        }
      })
      .catch(err => dispatch(errorFixedBidFetch("Error fetching fixed bid data")));
  };*/
};